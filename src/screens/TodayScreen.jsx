import { Page, Card } from '../kit/AppShell.jsx';

export function TodayScreen() {
  return (
    <Page>
      <Card title="Today">
        <p className="tt-muted">Today screen wiring up in next commit.</p>
      </Card>
    </Page>
  );
}
